from setuptools import setup, find_packages

setup(
    name='deepfake_detection_pkg',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List any external dependencies here
    ]
)